add_executable(06-matrices main.c)
target_include_directories(06-matrices PRIVATE ${FORGE_COMMON_DIR})
target_link_libraries(06-matrices PRIVATE SDL3::SDL3 $<$<NOT:$<C_COMPILER_ID:MSVC>>:m>)

add_custom_command(TARGET 06-matrices POST_BUILD
    COMMAND ${CMAKE_COMMAND} -E copy_if_different
        $<TARGET_FILE:SDL3::SDL3-shared>
        $<TARGET_FILE_DIR:06-matrices>
)
